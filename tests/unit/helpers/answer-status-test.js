import { answerStatus } from 'qa-board/helpers/answer-status';
import { module, test } from 'qunit';

module('Unit | Helper | answer status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerStatus([42]);
  assert.ok(result);
});
